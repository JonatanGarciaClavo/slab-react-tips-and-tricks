/**
 * With this hook we try to update document title with custom title provided in our component
 * @param {String} title
 */
const useTitle = title => {
  // 🔎 what kind of hook do you think will fit into this requirements 🤔 ?
  // document.title = title;
};

export default useTitle;
