import theme from 'mdx-deck/themes';

export default {
  // extends the default theme
  ...theme,
  // add a custom font
  font: `'Open Sans','Helvetica','Arial',sans-serif`,
  // custom colors
  colors: {
    text: '#222A36',
    background: '#FBFBFB',
  },
};
